# Rule-Hub TODO

## 端口规则范围优化

- [ ] 支持端口范围语法（如 `DST-PORT,8000-9000`）
- [ ] 支持多端口语法（如 `DST-PORT,80/443/8080`）

## 协议类规则

- [ ] 支持 `PROCESS-NAME` 规则
- [ ] 支持 `USER-AGENT` 规则

## 逻辑类规则

- [ ] 支持 `AND`、`OR`、`NOT` 逻辑运算符
- [ ] 支持规则组合（如 `AND,(DOMAIN,example.com),(DST-PORT,443)`）

## FINAL 大小写

- [ ] 统一输出为大写 `FINAL`（当前实现）
- [ ] 或提供配置选项支持小写 `final`

## 不支持规则过滤

- [ ] 支持 `RULE-SET` 规则转换（解析远程规则集内容）
- [ ] 支持 `SCRIPT` 规则转换或保留注释说明
- [ ] 提供过滤策略配置（丢弃 / 保留注释 / 转为占位符）
